const Skill = ({ skillName }) => {
    return (
        <>
        <h1 className="font-medium text-base tracking-wide ml-4 mt-2 cursor-pointer">{skillName}</h1>
        <hr className="mt-2"></hr>
        </>
    )
}

export default Skill;